<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug-only empty host: Compose instrumentation must run in the target app process. -->
        <activity
            android:name=".Phase6TestActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true" />
    </application>
</manifest>
